The codes implement the "ring sum", first exchaning one single element among processes (sum.c) and then exchaning a vector of elemnts (vectorsum.c). 

To compile use the Makefile.
In the vector version the dimension of the vector can be passed as a parameter (the default size is 4). 
